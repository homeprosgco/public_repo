import 'package:flutter/material.dart';

class BookingMobileListView extends StatelessWidget {
  const BookingMobileListView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}