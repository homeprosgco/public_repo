import { Injectable } from '@angular/core';
import { I{{ type_name }}Repository } from '../repositories/i-{{ split_and_hyphenate(type_name) }}.repository';
import { {{ type_name }} } from '../../data/models/{{ split_and_hyphenate(type_name) }}.interface';
import { Observable } from 'rxjs';

@Injectable({ providedIn: 'root' })
export class Update{{ type_name }}UseCase {
  constructor(private repo: I{{ type_name }}Repository) {}

  execute(input: {{ type_name }}): Observable<{{ type_name }}> {
    return this.repo.update{{ type_name }}(input);
  }
}