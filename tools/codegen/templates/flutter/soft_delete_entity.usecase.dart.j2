// GENERATED SOFT DELETE {{ type_name.upper() }} USE CASE
import '../repositories/i_{{ filename }}_repository.dart';
import '../../data/models/{{ filename }}.dart';

class SoftDelete{{ type_name }}UseCase {
  final I{{ type_name }}Repository repository;

  SoftDelete{{ type_name }}UseCase(this.repository);

  Future<{{ type_name }}Model> execute({{ type_name }}Model input) {
    return repository.softDelete{{ type_name }}(input);
  }
}
